<p id="notice"><%= notice %></p>

<h1>Givers</h1>

<table>
  <thead>
    <tr>
      <th>Cross street1</th>
      <th>Cross street2</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th>Country</th>
      <th>Zipcode</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @givers.each do |giver| %>
      <tr>
        <td><%= giver.cross_street1 %></td>
        <td><%= giver.cross_street2 %></td>
        <td><%= giver.latitude %></td>
        <td><%= giver.longitude %></td>
        <td><%= giver.country %></td>
        <td><%= giver.zipcode %></td>
        <td><%= link_to 'Show', giver %></td>
        <td><%= link_to 'Edit', edit_giver_path(giver) %></td>
        <td><%= link_to 'Destroy', giver, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Giver', new_giver_path %>
